Export as pdf -> drop into word. Can re-size and it maintains sharp edges, unlike png.

To set default formatting on new textboxes, format a textbox as desired then right click -> Set as default textbox
